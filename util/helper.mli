val percent_string : float -> string
(** [percent_string f] is [100] times the float value [f], with a percent sign string [%]
    appended at the end. *)

val identity : 'a -> 'a
(** [identity x] is the identity function. It will always return [x]. *)